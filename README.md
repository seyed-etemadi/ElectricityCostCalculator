# ElectricityCostCalculator

این پروژه یک برنامه‌ی ساده به زبان **C#** است که هزینه‌ی مصرف برق را بر اساس میزان مصرف کاربر محاسبه می‌کند.  
کاربر مقدار مصرف برق (کیلووات‌ساعت) را وارد می‌کند و برنامه با توجه به تعرفه‌ی مشخص، هزینه‌ی نهایی را نمایش می‌دهد.

## ویژگی‌ها
- دریافت میزان مصرف برق از کاربر (kWh)  
- محاسبه هزینه بر اساس تعرفه‌ی ثابت یا پلکانی (بسته به پیاده‌سازی)  
- نمایش نتیجه در محیط کنسولی  
- مدیریت خطا برای ورودی‌های نامعتبر  
- مناسب برای تمرین مفاهیم پایه‌ای سی‌شارپ مثل ورودی/خروجی، شرط‌ها و محاسبات عددی  

## مثال اجرا

لطفاً میزان مصرف برق (kWh) را وارد کنید:
250

هزینه‌ی برق مصرفی شما برابر است با: 125000 ریال
```

## نحوه استفاده
1. ریپازیتوری را کلون کنید:
   ```bash
   git clone https://github.com/YourUsername/ElectricityCostCalculator.git
   ```
2. پروژه را در **Visual Studio** باز کنید یا با دستور `dotnet run` اجرا کنید.  
3. میزان مصرف برق خود را وارد کنید تا هزینه‌ی نهایی محاسبه و نمایش داده شود.  

---

📌 این پروژه یک تمرین آموزشی برای یادگیری مفاهیم پایه‌ای **C#** مثل ورودی/خروجی، شرط‌ها و محاسبات عددی است و می‌تواند به عنوان نمونه‌ای ساده برای شبیه‌سازی محاسبه‌ی هزینه‌ی انرژی استفاده شود.  
